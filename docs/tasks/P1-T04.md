# P1-T04 — Message parser, TextState, MessageController

Status: passed
Assignee: Compose 2.5
Reviewer: Grok 4.6
Phase: 1

## 目的

エンジン非依存のメッセージ parser / 状態機械 / controller を移す。`MessageWindow` はまだ入れない。

## 移植元

commit `7cfd3156a5184193fe9e9e63958e5416d277e37e`（`/tmp/reusable_phaser_window_system` 可）。本リポジトリへ Phaser 版を入れない。

## 対象（ロジック変更禁止）

コピー:

- `src/message/types.ts`
- `src/message/MessageParser.ts`
- `src/message/TextState.ts`
- `src/message/MessageController.ts`
- `src/message/layoutPages.ts`
- `src/message/colorRuns.ts`
- `src/message/richTextStyles.ts`
- `src/message/sayPreflight.ts`

コピーしない: `src/message/MessageWindow.ts`

テスト:

- `tests/message/MessageParser.test.ts` — 全部
- `tests/message/TextState.test.ts` — 全部
- `tests/message/MessageController.test.ts` — 全部
- `tests/message/layoutPages.test.ts` — 全部
- `tests/message/autoPageLayout.test.ts` — 全部
- `tests/message/richTextStyles.test.ts` — `describe("MessageWindow rich-text contract")` は **捨てる**。`collectPageFlatStyles` / `richTextFromFlat` は残す
- `tests/message/messageExtensions.test.ts` — WindowBase / MessageWindow のソース検査 describe は **捨てる**。`splitLineColorRuns` と `assertMessageSayPreflight` など純契約テストは残す

`src/index.ts` が今回 export してよいもの:

- types: `MessageToken`, `MessageParseResult`, `MessagePortraitOptions`, `MessageAudioHooks`, `TextState`, `TextStateEffect`, `TextStateStepResult`, `MessageStartRequest`, `MessageRenderSnapshot`
- `MissingMessagePortraitError`, `MessageBusyError`
- `parseMessage`
- `createInitialTextState`, `reduceTextState`, `getRevealedText`, `getRevealedPageText`, `getRevealedPageColors`, `requiresAdvanceInput`
- `MessageController`

export しない: `sayPreflight`, `assertMessageSayPreflight`, `splitLineColorRuns`, `computeLayoutPageBreaks`, `collectPageFlatStyles`, `MessageWindow`, `MessageSayOptions`, `MessageWindowOptions`

更新: `test:unit` に `tests/message`、public-exports、consumer（MessageWindow 禁止）、`src/README.md`、`docs/API.md`。`MessageWindow.say` の利用例は置かない。

## 非対象

- MessageWindow / TextWindowBase / Pixi BitmapText
- phaser / pixi.js import
- PROGRESS 改稿、git commit

## 受入条件

1. `bun run check` が exit 0
2. parser / TextState / MessageController の unit test が通る
3. `src/message/**` に `phaser` も `pixi.js` も無い
4. `MessageWindow` が無く、internal helper が barrel に無い

## 実行コマンド

```bash
bun run check
```
