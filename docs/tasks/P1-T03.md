# P1-T03 — Text layout, rich text, font fallback

Status: passed
Assignee: Compose 2.5
Reviewer: Grok 4.6
Phase: 1

## 目的

エンジン非依存のテキスト layout / RichText / font metrics / fallback を、fake measurer の unit test 付きで移す。`TextWindowBase` と Pixi/Phaser BitmapText measurer はまだ入れない。

## 移植元

commit `7cfd3156a5184193fe9e9e63958e5416d277e37e`（`/tmp/reusable_phaser_window_system` 可）。本リポジトリへ Phaser 版を入れない。

## 対象（ロジック変更禁止）

コピー:

- `src/text/types.ts`
- `src/text/TextLayout.ts`
- `src/text/richText.ts`
- `src/text/fontMetrics.ts`
- `src/text/fontFallback.ts`
- `src/text/stackedText.ts`
- `src/text/adaptBitmapTextMeasurer.ts`
- `src/text/BitmapFontAsset.ts`（URL 記述子のみ。Phaser loader は使わない）

`src/text/FallbackBitmapTextMeasurer.ts`:

- `FallbackBitmapTextMeasurer` **クラス**はそのままコピー
- 末尾の `createBitmapTextMeasurer(scene: Phaser.Scene, ...)` は **コピーしない**（Phaser 型。Pixi 版 factory は P3-T01）
- クラス本体から `PhaserBitmapTextMeasurer` import を消す（クラスは chain の `OwnedBitmapTextMeasurer` だけ使う）

コピーしない:

- `src/text/TextWindowBase.ts`
- `src/text/PhaserBitmapTextMeasurer.ts`

テスト（fake measurer のものだけ）:

- `tests/text/TextLayout.test.ts` — 全部
- `tests/text/layoutRichText.test.ts` — 全部
- `tests/text/richText.test.ts` — 全部
- `tests/text/fontMetrics.test.ts` — `describe("fontMetrics isolation")`（font.xml と `node_modules/phaser` を読む）は **捨てる**。残りはコピー
- `tests/text/fontFallback.test.ts` — `describe("font fallback isolation")`（WindowBase / TextWindowBase / SelectableWindow のソース検査）は **捨てる**。残りはコピー
- `tests/text/TextWindowBase.test.ts` — **コピーしない**

`src/index.ts` が今回 export してよいもの（移植元と同じ名前）:

- types: `BitmapTextMeasurer`, `BitmapTextMeasureStyle`, `BitmapTextMeasurement`, `BitmapFontNativeMetrics`, `ScaledFontMetrics`, `LayoutLine`, `LayoutLineRun`, `TextLayoutResult`, `TextLayoutOptions`, `TextAlign`, `RichTextSpan`, `RichText`, `WindowTextContent`, `BitmapFontAsset`
- errors: `MissingBitmapGlyphError`, `BitmapFontNotLoadedError`, `FontSwapBusyError`
- `layoutText`, `layoutRichText`
- `DEFAULT_BITMAP_FONT_ASSET`
- `FallbackBitmapTextMeasurer`

export しない: `flattenRichText`, `splitTextFontRuns`, `stackedTextHeight`, `scaleFontMetrics`, `adaptBitmapTextMeasurer`, `createBitmapTextMeasurer`, `PixiBitmapTextMeasurer`, `TextWindowBase`

更新:

- `package.json` `test:unit` に `tests/text`
- `tests/package/public-exports.test.ts`
- `examples/consumer` — `layoutText` / `layoutRichText` の型検査は可。BitmapText の実体は使わない
- `src/README.md` に `text/`
- `docs/API.md` Current surface。`TextWindowBase` / `PixiBitmapTextMeasurer` は Planned

## 非対象

- font.png / font.xml の同期
- `Assets.load`、Pixi `BitmapText`
- WindowBase
- phaser / pixi.js import
- 名称変更、PROGRESS 改稿、git commit

## 受入条件

1. `bun run check` が exit 0
2. layout / richText / fallback の unit test が fake measurer で通る
3. `src/text/**` に `phaser` も `pixi.js` も無い
4. `flattenRichText` と `createBitmapTextMeasurer` が公開 barrel に無い
5. `TextWindowBase` が無い

## 実行コマンド

```bash
bun run check
```
