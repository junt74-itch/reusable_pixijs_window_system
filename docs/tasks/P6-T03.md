# P6-T03 — README / SUBMODULE / MIGRATION / API / SPEC 現行化

Status: passed
Assignee: Compose 2.5
Reviewer: Grok 4.6
Phase: 6

## 目的

導入文書を **いまの landed surface** に合わせる。Phase 0 の「VERSION のみ」「いまは import できません」を残さない。公開 API と sandbox 経路は増やさない。`src/` の実装は変えない。npm publish はまだやらない。

## 導入経路（README にこの順で書く）

1. `bun install` / `bun run check`
2. 既定 sandbox: `bun run dev` → http://localhost:5173/ （say → choose）
3. Three.js **標準**: http://localhost:5173/three-overlay.html（別 canvas）
4. Three.js **任意**: http://localhost:5173/three-shared-context.html（共有 context。標準ではない）
5. submodule: [docs/SUBMODULE.md](../SUBMODULE.md)（入口は root `index.ts`）
6. Phaser からの破壊的変更: [docs/MIGRATION.md](../MIGRATION.md)

公開 symbol の一覧は [docs/API.md](../API.md) Current surface を正とする。新しい window 種や `?scene=` カタログは作らない。

## 直す文書（これ以外の実装ファイルは触らない）

| ファイル | やること |
|---|---|
| `README.md` | 冒頭「Phase 5 進行中」をやめる。上記 1–6 を導入の正とする。sandbox キー表は残してよい |
| `docs/README.md` | 「SPEC/API は移植完了時の規範＋Phase 0 未実装」を現行化。索引の説明を landed に合わせる |
| `docs/SUBMODULE.md` | 「Phase 0 / VERSION のみ」「PixiWindowInput は未実装」を削除。barrel からの Window / Host / Input import を例示（動くゲームは書かない。`examples/consumer/submodule-source.ts` に合わせて型 import 中心でよい） |
| `docs/MIGRATION.md` | 「予告」をやめる。Breaking 8 項は残す。NineSlice / `bindFocusControllerToHost` / overlay vs shared を 1 節で現状化 |
| `docs/SPECIFICATION.md` | 各節の `Status: unimplemented` を landed / 対象 Phase 済みに直す。冒頭「VERSION と最小 sandbox のみ」を削除。Verification を Phase 6 文書現行化まで含む |
| `docs/API.md` | 「Planned surface（未実装）」「いまは import できません」を削除または Rename 一覧＋Defer へのポインタだけにする。Current surface は既にあるので重複定義しない。`?scene=` を未実装のまま残すなら BACKLOG へ一文（カタログを実装しない） |
| `docs/PIXI_PORT_CHECKLIST.md` | Phase 6 の文書現行化を反映。npm publish はチェックしない |
| `src/README.md` | 公開 module 説明が現行と食い違っていれば直す。コードは変えない |

`docs/BACKLOG.md` の「共有 context を標準にするか」は **残す**。

## テスト

実装コードのテストは増やさない。既存 `tests/package/repository-access.test.ts` を壊さない。

追加してよい（任意だが推奨）`tests/package` または `tests/examples` の **文書契約 1 ファイル**:

1. `README.md` が `bun run dev` と `three-overlay.html` と `docs/SUBMODULE.md` を含む
2. `docs/SUBMODULE.md` が `VERSION のみ` を含まない
3. `docs/API.md` が `いまは import できません` を含まない
4. `docs/SPECIFICATION.md` が `公開コードは VERSION と最小 sandbox のみ` を含まない
5. `docs/MIGRATION.md` が `予告` を含まない

WebGL / Application.init 禁止。`src/` 禁止。consumer の Window インスタンス化デモ禁止。

## 非対象

- `src/**` の振る舞い変更
- 公開 barrel への新規 export
- `?scene=` カタログ、NineSlice PNG コピー
- npm publish / release tag
- 共有 context を標準にすること
- `examples/main.ts` / overlay / shared の置換
- PROGRESS、git commit

## 受入条件

1. `bun run check` が exit 0
2. README から sandbox・overlay・submodule・MIGRATION に辿れる
3. SUBMODULE / SPEC / API が Phase 0 の「未実装」表現を残していない
4. ライブラリコードと既定 sandbox が変わっていない

## 実行コマンド

```bash
bun run check
```
