# P1-T05 — Selection, scroll, viewport, focus, a11y

Status: passed
Assignee: Compose 2.5
Reviewer: Grok 4.6
Phase: 1

## 目的

エンジン非依存の SelectionController / ScrollController / viewport layout / WindowFocusController / bindWindowA11y を移す。Window 描画クラスはまだ入れない。

## 移植元

commit `7cfd3156a5184193fe9e9e63958e5416d277e37e`（`/tmp/reusable_phaser_window_system` 可）。

## 対象（ロジック変更禁止）

コピー:

- `src/selection/types.ts`, `cursorBlink.ts`, `SelectionController.ts`
- `src/scroll/types.ts`, `ScrollController.ts`, `scrollInputBinding.ts`, `scrollVisibility.ts`, `scrollChrome.ts`
- `src/layout/viewportLayout.ts`
- `src/focus/types.ts`, `WindowFocusController.ts`
- `src/a11y/types.ts`, `bindWindowA11y.ts`

コピーしない:

- `SelectableWindow.ts`, `CursorRenderer.ts`
- `ScrollableWindow.ts`, `ScrollbarRenderer.ts`, `ScrollContentClip.ts`, `ScrollOverflowIndicators.ts`
- `bindSceneShutdown.ts`

テスト:

- `tests/selection/SelectionController.test.ts` — 全部
- `tests/scroll/ScrollController.test.ts` — 全部
- `tests/layout/viewportLayout.test.ts` — `describe("viewport layout isolation")` は **捨てる**（WindowBase 未移植）
- `tests/focus/WindowFocusController.test.ts` — `describe("WindowFocusController isolation")` は **捨てる**（WindowBase / bindSceneShutdown / sandbox scene）
- `tests/a11y/bindWindowA11y.test.ts` — `WindowBase has no a11y...` のテストは **捨てる**。`a11y module stays Phaser-free...` と振る舞いテストは残す
- `SelectableWindow.test.ts`, `CursorRenderer.test.ts`, `ScrollableWindow.test.ts` — **コピーしない**

`src/index.ts` が今回 export してよいもの:

- `SelectableItem`, `SelectionControllerOptions`, `SelectionController`, `cursorBlinkVisible`
- `ScrollController`, `bindScrollInput`, `ScrollAxis`, `ScrollBounds`, `ScrollChangeListener`, `ScrollChangeSubscription`, `ScrollControllerOptions`
- `layoutWindowInViewport`, `ViewportAnchor`, `ViewportLayoutRequest`
- `WindowFocusController`, `WindowFocusError`, `FocusableWindow`, `FocusAcquireOptions`, `FocusSnapshot`, `FocusChangeListener`, `FocusChangeSubscription`
- `bindWindowA11y` と a11y types（移植元 barrel と同じ）

export しない: `SelectableWindow`, `CursorRenderer`, `ScrollableWindow`, `ScrollbarRenderer`, `bindFocusControllerToScene`, `bindFocusControllerToHost`, scrollChrome helpers

更新: `test:unit` に `tests/selection tests/scroll tests/layout tests/focus tests/a11y`、public-exports、consumer（Window クラス禁止）、`src/README.md`、`docs/API.md`。

## 非対象

- Window 描画、Pixi/Phaser import、commandItems、stickToLatest（別タスク）
- PROGRESS 改稿、git commit

## 受入条件

1. `bun run check` が exit 0
2. 上記 controller の unit test が通る
3. 新規 `src/**` に `phaser` も `pixi.js` も無い
4. SelectableWindow / ScrollableWindow / CursorRenderer が無い
5. `bindFocusControllerToScene` が barrel に無い

## 実行コマンド

```bash
bun run check
```
